apiVersion: v1
kind: Service
metadata:
  name: iot-app-service
  namespace: dev
  labels:
    app: iot-app
spec:
  type: ClusterIP
  ports:
  - port: 8888
    targetPort: 8888
    protocol: TCP
  selector:
    app: iot-app
